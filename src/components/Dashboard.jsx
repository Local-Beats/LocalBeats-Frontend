import React from 'react';
import Activelistener from './ActiveListener';

const Dashboard = () => {
    return (
        <main>
            <h1>Dashboard</h1>
            <Activelistener />
        </main>
    )
};

export default Dashboard;